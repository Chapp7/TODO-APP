const initialState = {
  tasks:[
    {id: '1', title: 'this is task 1',content:'it should becompleted by tommorrow'}
  ]
}
  

const taskReducer =(state = initialState,action)=>{
  return state
}
export default taskReducer